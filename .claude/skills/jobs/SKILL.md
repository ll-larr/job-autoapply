---
name: jobs
description: Управление конвейером откликов job-autoapply — поиск вакансий, просмотр очереди, панель одобрения, отправка. Использовать, когда пользователь просит найти вакансии, посмотреть очередь, открыть панель, отправить отклики, или пишет /jobs.
---

# Конвейер откликов

Полуавтоматическая подача резюме. Система ищет вакансии, отсеивает нерелевантные,
пишет сопроводительное под каждую и кладёт в очередь. **Отправляет только то, что
человек одобрил руками.**

Репозиторий: `C:\Users\lar\job-autoapply`.

## Железные правила

1. **Никогда не запускай `send` без явной просьбы пользователя в текущем разговоре.**
   Отклик на hh.ru необратим: отозвать нельзя, работодатель видит его сразу. Одна
   сессия уже отправила отклик по своей инициативе, разрешив запрос, о назначении
   которого только догадывалась по его имени. Правило оттуда: **имя эндпоинта ничего
   не доказывает**, модифицирующий запрос считается необратимым, пока не доказано
   обратное.
2. **Капчу не обходить.** Обнаружил — остановись и позови человека.
3. **Пароли не вводить.** Аутентификация только через профиль, куда пользователь
   залогинился сам (`npm run login`, если профиль протух).
4. **Письма — творческий выход пользователя.** Молчание не считается одобрением.
   Если он не оценил письма, спроси прямо.

## Перед любым запуском

Три вещи ломают прогон чаще всего. Проверь их, прежде чем диагностировать что-то ещё.

**Ключ.** Читается из `OPENROUTER_API_KEY`. Переменная выставлена в User-области,
и в уже открытом терминале её может не быть:

```powershell
$env:OPENROUTER_API_KEY = [Environment]::GetEnvironmentVariable("OPENROUTER_API_KEY","User")
```

Без ключа прогон не падает: вакансии попадут в очередь с пустыми письмами.

**Прокси.** Node не ходит через `HTTP_PROXY` сам. Без этого запросы к OpenRouter и
hr.ge возвращают `403 "Access denied by security policy"` — это блок-страница
провайдера, **не ошибка API и не проблема ключа**. Отличить легко: `curl` к тому же
адресу проходит, Node нет. В npm-скриптах уже стоит `tsx --use-env-proxy`; прокси
(xray) должен слушать на `127.0.0.1:10801`.

**Залипший браузер.** `launchPersistentContext` держит каталог профиля эксклюзивно.
Прерванный прогон оставляет Chromium висеть, и следующий падает с «профиль занят»:

```powershell
Get-Process chrome | Where-Object { $_.Path -like "*ms-playwright*" } | Stop-Process -Force
```

## Команды

| Задача | Команда |
|---|---|
| Найти вакансии и наполнить очередь | `npm run search -- "бизнес-аналитик" --limit 6` (6 — сколько попадёт в очередь) |
| Панель одобрения | `npm run panel` → http://127.0.0.1:4321 |
| Отправить одобренное | `npm run send` |
| Что в очереди | `npm run status` |
| Аварийная остановка | `npm run stop` |
| Перелогиниться | `npx tsx scripts/login.ts` (hh) / `... careerist` |

Повседневный цикл целиком — `Запустить.cmd` в корне проекта: проверяет окружение,
чистит залипший браузер, ищет, открывает панель. Отправки там нет намеренно.

`--limit` — это ЗАКАЗ, а не потолок просмотра: «набери столько вакансий в
очередь». Поиск сам решает, сколько выдачи для этого прочитать, листает
порциями и делит заказ поровну между формулировками запроса; если у одной
формулировки выдача кончилась, её долю добирают остальные. Предохранитель от
бесконечного листания — потолок просмотра (сорок карточек на каждую заказанную,
но не меньше двухсот); когда прогон упёрся в него или в конец выдачи, он
называет это в отчёте, чтобы недобор не выглядел как «больше ничего нет».

То же число вводится в панели, в правом верхнем углу.

## Что показывать пользователю

Очередь смотрится в панели, а не в базе. Если он спрашивает «где очередь» — запусти
`npm run panel` и открой `http://127.0.0.1:4321`.

Для быстрой сводки без панели:

```powershell
node --use-env-proxy -e "const{DatabaseSync}=require('node:sqlite');const d=new DatabaseSync('data/queue.db');for(const r of d.prepare('select id,status,score,length(letter) ll,vacancy_json from applications order by score desc').all()){const v=JSON.parse(r.vacancy_json);console.log(`#${r.id} [${r.status}] скор=${r.score} письмо=${r.ll} :: ${v.title.slice(0,55)}`)}"
```

## Прокси: раздельная маршрутизация

Проверено живьём 2026-09-01, по каждому адресату отдельно:

| адресат | напрямую | через прокси |
|---|---|---|
| OpenRouter (письма) | **403 блок-страница** | 200 |
| hh.ru | 200 | **403** |
| careerist.ru | 200 | **ConnectTimeout, 3 из 3** |
| hr.ge | 200 | 200 |

То есть **ни «всё через прокси», ни «всё напрямую» не работает.** Прокси нужен
ровно одному адресату — OpenRouter; площадкам он ломает связь.

Решение — `NO_PROXY` со списком площадок плюс `HTTP_PROXY`/`HTTPS_PROXY`. С этой
парой все четыре отвечают 200 одновременно. Переменные задают лаунчеры
(`scripts/panel.ps1`, `run.ps1`, `send.ps1`): в User- и Machine-области их нет,
а двойной клик по `.cmd` не наследует окружение терминала.

Node не читает их без `--use-env-proxy` — флаг стоит в npm-скриптах. Выставить
его или переменные из кода поздно: undici читает конфигурацию один раз при
старте процесса.

**Браузер — отдельная история.** Chromium берёт прокси из НАСТРОЕК WINDOWS, а
не из переменных окружения, и у владельца там постоянно включён VPN-клиент
(`ProxyEnable=1`, `127.0.0.1:10801`). Через него careerist.ru в браузере даёт
`ERR_TIMED_OUT` — то есть подача на careerist ломалась бы молча. Поэтому
профиль запускается с `--no-proxy-server` (`profileLaunchOptions` в
src/browser.ts): браузер ходит только на площадки, прокси ему не нужен
никогда. `proxy: { server: 'direct://' }` для этого НЕ годится — Playwright
его принимает, Chromium отвечает `ERR_PROXY_CONNECTION_FAILED`.

Итого граница проходит по «браузер против fetch»: браузер всегда напрямую,
fetch — через прокси, кроме площадок в `NO_PROXY`.

**Короткий ответ на «работает ли без прокси»:** работает всё, кроме генерации
писем. Поиск, скоринг, фильтры, панель и подача — не трогают OpenRouter.

## Площадки

Подключены: **hh.ru** (поиск и подача), **hr.ge** (поиск), **careerist.ru** (поиск и подача).

Разведка 2026-08-31 по остальным из изначального списка — не повторяй её заново,
если пользователь спросит «а почему нет такого-то»:

- **remotejob.ru** — не отвечает. TLS-рукопожатие рвётся, http отдаёт 503, браузер
  тоже не проходит. Адаптера нет намеренно: проверить его сейчас нечем.
- **geekjob.ru** — сайт про разработку. По «аналитик», «бизнес-аналитик», «analyst»
  вакансий ноль; поиск JS-овый, параметр `qs` в HTTP-ответе ничего не фильтрует.
  Адаптер принёс бы пустоту.
- **careerspace.app** — работает и вакансии по профилю там есть, но анонимному
  посетителю отдаётся ровно 8 штук; всё остальное за логином, `/api/v1/jobs/filter`
  без токена отвечает 403. Свободного текстового поиска у площадки нет вообще,
  фильтры только по направлению (`Аналитика` — function_id 13), уровню и зарплате.
  Строить имеет смысл ПОСЛЕ того, как пользователь заведёт аккаунт.

## Фильтры отбора

Три исходных (опыт 1–3 / грейд «ведущий» и выше / 1С) плюс четыре, выведенные из
разбора отменённых вакансий 2026-09-01:

- **Платформа.** 1С и Битрикс24 — когда вакансия построена вокруг них. «Аналитик
  битрикс — я его не знаю».
- **Заголовок обязан называть аналитика.** Скор говорит, ЧЕМ занимаются;
  заголовок — кем зовут. «Менеджер по операционному консалтингу» набирал
  проходной скор процессной лексикой в описании. Гейт строгий и это плата:
  он же отсеивает «Координатора операционных проектов», на которого владелец
  откликнулся. Список принимаемых слов — `ANALYST_TITLE_PATTERNS`, расширяется
  одной строкой.
- **Стажировки** отсекаются. Это ОТМЕНЯЕТ прежнее правило письма «при отклике на
  стажировку сказать, что рассматриваю junior+/middle» — откликаться на них
  больше не будем.
- **Выше junior — только для запросов с `juniorOnly`.** «Старший» отсекается там,
  где стоит это ограничение (сейчас «системный аналитик»), и НЕ отсекается
  везде: владелец отправил отклик на «Старший ИТ аналитик» в тот же день, когда
  отменил «Старший системный аналитик».

## Известные пробелы

Говори о них честно, не выдавай систему за законченную.

- **Капча и «вы уже откликались» у hh.ru не распознаются.** Маркеры не снимались
  живьём, а выдуманный селектор хуже отсутствующего: он молча не совпадёт никогда.
  Вместо этого стоит предохранитель — три отказа подряд по одной площадке
  останавливают прогон.
- **Подача на hr.ge не реализована.** Возвращает честный отказ со ссылкой «подай
  вручную»: снять контракт значит отправить настоящий отклик работодателю.
- **careerist.ru умеет и искать, и подавать** (с 2026-08-31, пользователь
  зарегистрировался и залогинился). Письмо там уходит ВМЕСТЕ с откликом — приятное
  отличие от hh.ru. Успех проверяется по разделу `/responds/`, а не по строке в
  ответе. Живой подачи ещё не было ни одной: первая случится, когда пользователь
  нажмёт «Отправить всё» в панели.
- **Письмо на hh.ru уходит вторым шагом**, отдельным сообщением в чат. Маршрута, где
  оно уходит вместе с откликом, у hh.ru нет — проверено.
- **Пороги не откалиброваны.** `letterFullThreshold` равен 75, а живые скоры выходят
  52–58, то есть режим `full` пока не включается ни разу.

## Если что-то сломалось

Смотри `docs/hh-selectors.md`, `docs/hrge-api.md` и `docs/careerist-selectors.md` —
они написаны по живым перехватам, в них ничего не угадано. Ход работ и находки ревью — в `.superpowers/sdd/progress.md`.
